public interface Visitor {
    double calculateSmartphoneDiscount(Smartphone smartphone);
    double calculateComputerDiscount(Computer computer);
    double calculateHeadsetDiscount(Headset headset);
}
